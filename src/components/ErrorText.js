import styled from "styled-components";

 const ErrorText = styled.p`
  font-size: 24px;
  margin: 0 auto;
  line-height: 1.5;
  color: var(--dark-blue);
  font-weight: 600;
`;

export default ErrorText