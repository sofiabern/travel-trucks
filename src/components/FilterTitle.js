import styled from "styled-components";

const FilterTitle = styled.h3`
  font-size: 20px;
  font-weight: 500;
  line-height: 1.2;
  color: var(--dark-blue);
  margin-bottom: 49px;
  }
`;

export default FilterTitle;
